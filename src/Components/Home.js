import React from 'react';
import { Button } from '@material-ui/core';

const Home = () => {
    return (
        <div>
            <Button>Hello</Button>
        </div>
    );
}

export default Home;
